---
/**
 * Entrada del panel.
 *
 * Redirige al listado en lugar de crear un dashboard vacio con metricas que
 * todavia no existen.
 */
export const prerender = false;

return Astro.redirect('/admin/propiedades', 302);
---
