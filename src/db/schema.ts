/**
 * Esquema de base de datos de CodeLoba (Cloudflare D1 / SQLite).
 *
 * Fase 0: intencionadamente vacio.
 *
 * El modelo de datos (propiedades, imagenes, etc.) se definira en la
 * siguiente fase usando los helpers de `drizzle-orm/sqlite-core`:
 *
 *   import { sqliteTable, integer, text } from 'drizzle-orm/sqlite-core';
 *
 * Cada cambio en este archivo se convierte en una migracion SQL dentro de
 * `drizzle/` ejecutando `npm run db:generate`.
 */

export {};
